# https://doc.rust-lang.org/cargo/reference/audit-toml.html

[advisories]
ignore = [
    # RUSTSEC-2023-0071: Marvin Attack on `rsa` crate (timing side-channel in decryption).
    # We only use `rsa` for RS256 signature *verification* (public-key operations),
    # which is not affected by this advisory. No fixed version is available.
    "RUSTSEC-2023-0071",
]
